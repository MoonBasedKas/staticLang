using System.Collections;
namespace action{
    class operation{
        private ArrayList tokens;

        /**
        * Determines what to execute and performs the appropriate sub oepration.
        */
        public int execute(){

            return 0;
        }
    }
}